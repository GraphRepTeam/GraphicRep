************** SETTING INTERPRETER **************
It is enough to install a generic environment, maybe using conda, with the following packages: xlrd, networkx.

************** STARTING FILE **************
Main file to start is gdna_main.py, so it has to be the file to execute.

I suggest to use PyCharm